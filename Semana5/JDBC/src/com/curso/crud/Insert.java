package com.curso.crud;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.Statement;

public class Insert {
	/*
	private static final  String URL = "jdbc:mysql://localhost:3306/bdcurso";
	private static final String USER = "root";
	private static final String PWD = "root";
	private static final String QUERY =("INSERT INTO articulo (codigo, nombre, precio,fabricante)VALUES (last_insert_id()+1,'Altavoces', '70','2');");
*/
	String url = "jdbc:mysql://localhost:3306/bdcurso";
	String user = "root";
	String pwd = "root";
	/*String query =("INSERT INTO articulo (codigo, nombre, precio,fabricante)VALUES (last_insert_id()+1,'Altavoces', '70','2');");
	Connection conexion = DriverManager.getConnection(url,user,pwd);
	Statement sentencia = conexion.createStatement();*/
	
}

